permissions:
  contents: write

name: Update Homepage with All Sections

on:
  schedule:
    - cron: '5 * * * *'  # हर घंटे 5 मिनट पर चलेगा
  workflow_dispatch:

jobs:
  update-homepage:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
      with:
        fetch-depth: 0

    - name: Fetch RSS feeds
      run: |
        curl -s https://www.jagranjosh.com/rss/education.xml -o education.xml
        curl -s https://www.jagranjosh.com/rss/jobs.xml -o jobs.xml
        curl -s https://www.hindustantimes.com/feeds/rss/india-news/rssfeed.xml -o current.xml

    - name: Extract headlines with fallback
      run: |
        EDU=$(grep -oP '(?<=<title>).*?(?=</title>)' education.xml | sed -n '2p')
        JOB=$(grep -oP '(?<=<title>).*?(?=</title>)' jobs.xml | sed -n '2p')
        CUR=$(grep -oP '(?<=<title>).*?(?=</title>)' current.xml | sed -n '2p')

        [ -z "$EDU" ] && EDU="⚠️ कोई एजुकेशन हेडलाइन नहीं मिली"
        [ -z "$JOB" ] && JOB="⚠️ कोई जॉब हेडलाइन नहीं मिली"
        [ -z "$CUR" ] && CUR="⚠️ कोई करेंट अफेयर्स हेडलाइन नहीं मिली"

        echo "📚 Education: $EDU"
        echo "💼 Jobs: $JOB"
        echo "📰 Current Affairs: $CUR"

    - name: Inject content into index.html
      run: |
        # 🔍 Education
        EDU_BLOCK="<details><summary>$EDU</summary><p>यह खबर JagranJosh से ली गई है।</p><a href='education.html'>विस्तार से पढ़ें...</a></details>"
        sed -i "/<!--EDUCATION_PLACEHOLDER-->/a $EDU_BLOCK" index.html

        # 🔍 Jobs
        JOB_BLOCK="<details><summary>$JOB</summary><p>यह जानकारी JagranJosh से ली गई है।</p><a href='jobs.html'>विस्तार से पढ़ें...</a></details>"
        sed -i "/<!--JOBS_PLACEHOLDER-->/a $JOB_BLOCK" index.html

        # 🔍 Current Affairs
        CUR_BLOCK="<details><summary>$CUR</summary><p>यह खबर Hindustan Times से ली गई है।</p><a href='current-affairs.html'>विस्तार से पढ़ें...</a></details>"
        sed -i "/<!--CURRENT_AFFAIRS_PLACEHOLDER-->/a $CUR_BLOCK" index.html

        # 🔄 Timestamp Update
        DATE=$(date +"%d %B %Y, %I:%M %p")
        sed -i "s|🔄 अंतिम अपडेट:.*|🔄 अंतिम अपडेट: $DATE|" index.html

    - name: Commit and push changes
      run: |
        git config --global user.name 'github-actions[bot]'
        git config --global user.email 'github-actions[bot]@users.noreply.github.com'

        echo "📌 Staging all modified files..."
        git add index.html

        if git diff --cached --quiet; then
          echo "✅ Homepage already up to date. No commit needed."
        else
          COMMIT_MSG="🤖 Homepage Updated: $EDU | $JOB | $CUR"
          echo "✅ Committing with message: $COMMIT_MSG"
          git commit -m "$COMMIT_MSG"
          git push origin main
          echo "🎉 Push successful!"
        fi