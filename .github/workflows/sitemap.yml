name: Refresh Sitemap

on:
  schedule:
    - cron: '15 * * * *'  # हर घंटे 15 मिनट पर ऑटोमैटिक चलेगा
  workflow_dispatch:        # मैन्युअली भी चला सकता है

jobs:
  update-sitemap:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Update lastmod in sitemap.xml
      run: |
        # 🕒 Generate current IST timestamp
        NOW=$(date +"%Y-%m-%dT%H:%M:%S+05:30")

        # 🔧 Replace all <lastmod> values
        sed -i "s|<lastmod>.*</lastmod>|<lastmod>$NOW</lastmod>|g" sitemap.xml

        echo "🗺️ Sitemap updated with lastmod: $NOW"

    - name: Commit and push sitemap
      run: |
        git config --global user.name 'github-actions[bot]'
        git config --global user.email 'github-actions[bot]@users.noreply.github.com'

        git add sitemap.xml
        if git diff --cached --quiet; then
          echo "✅ Sitemap already up to date. No commit needed."
        else
          git commit -m "🗺️ Sitemap refreshed: $NOW"
          git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}
          git push
          echo "🎉 Sitemap pushed successfully!"